# Figures

Visualizations are generated directly inside the executed Jupyter notebook, including class distribution, model evaluation charts, confusion matrix, ROC curve, and feature importance output.
